{
  "name": "gulp-check-include",
  "version": "1.0.0",
  "description": "Filter files by content",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "plugin-error": "^1.0.1",
    "through2": "^3.0.0"
  }
}
